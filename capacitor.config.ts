import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeremytodo.com',
  appName: "Jeremy's Todo",
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
